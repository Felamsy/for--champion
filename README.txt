CHAMPION WEBSITE

Open index.html in a browser to preview the website.

Next step: add the real photos/videos into an assets folder and replace/add <img> and <video> elements in index.html. The site is static HTML/CSS/JS, so it can be hosted free on GitHub Pages.

The writing is intentionally personal and human; we can keep refining it as more memories/photos are added.
